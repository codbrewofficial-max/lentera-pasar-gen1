NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/v1
# local-subpath = routes look like /site-slug/about
# domain = routes look like /about, useful later for per-domain/subdomain rendering
NEXT_PUBLIC_SITE_RENDERER_MODE=local-subpath
